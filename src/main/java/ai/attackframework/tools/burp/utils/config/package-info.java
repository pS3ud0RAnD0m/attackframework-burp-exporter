/**
 * Utilities for serializing/deserializing the extension's configuration.
 * Kept separate from filesystem helpers to maintain clear package boundaries.
 */
package ai.attackframework.tools.burp.utils.config;
